<h1> <%= @company.name %> </h1>
<h3> All Employees </h3>
<ul>
    <% @company.employees.each do |employee| %>
        <li> <%= link_to employee.name, employee_path(employee) %> </li>
    <% end %>
</ul>